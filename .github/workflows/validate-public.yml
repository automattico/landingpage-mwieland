name: CI Gate

on:
  push:
  pull_request:

jobs:
  prod-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run production gate
        run: ./scripts/prod-gate.sh
